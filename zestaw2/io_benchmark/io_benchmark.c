#include <stdio.h>
#include "file_manip.h"

void parse_args(int argc, char **argv, int *num_records, int *record_size, char **filename, Mode *mode, FunctionType *type);

int main(int argc, char **argv)
{
    int num_records, record_size;
    char *filename;
    Mode mode;
    FunctionType type;
    return 0;
}
